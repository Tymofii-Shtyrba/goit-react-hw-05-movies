import styled from "@emotion/styled"


const StyledCast = styled.div`
margin: 0 auto;
padding : 15px;
max-width: ${200 * 5 + 10 * 4}px;

ul {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}
`

export default StyledCast;